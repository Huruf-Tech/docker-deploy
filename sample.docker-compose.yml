version: "3.9"

services:
  web:
    build: .
    image: __image__
    container_name: my-app-__environment__
    ports:
      - "80:8080"
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"